# Spring Security + JWT — Reusable Skeleton & Mental Model

> Save this file. Whenever a new project needs login/authentication, copy these files in, rename the package, and adjust field names.

---

## The Mental Model (memorize this order — it never changes)
